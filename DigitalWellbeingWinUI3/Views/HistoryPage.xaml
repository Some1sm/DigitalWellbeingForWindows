<Page
    x:Class="DigitalWellbeingWinUI3.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DigitalWellbeingWinUI3.Views"
    xmlns:vm="using:DigitalWellbeingWinUI3.ViewModels"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Start Date"/>
                <CalendarDatePicker Date="{Binding StartDate, Mode=TwoWay}"/>
            </StackPanel>

            <StackPanel>
                <TextBlock Text="End Date"/>
                <CalendarDatePicker Date="{Binding EndDate, Mode=TwoWay}"/>
            </StackPanel>
            
            <Button Content="Generate Chart" Command="{x:Bind ViewModel.GenerateChartCommand}" VerticalAlignment="Bottom"/>

            <ToggleSwitch Header="View by Tag" IsOn="{Binding IsTagView, Mode=TwoWay}" VerticalAlignment="Bottom"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <lvc:PieChart Series="{Binding ChartSeries}" LegendPosition="Right"/>
            
            <ProgressRing IsActive="{Binding IsLoading}" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

    </Grid>
</Page>
